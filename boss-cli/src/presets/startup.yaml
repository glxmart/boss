name: Startup
description: Fast iteration, minimal gates

gates:
  coverage: 60
  lint: true
  typecheck: true
  test: true

git_hooks:
  pre_commit:
    - lint-staged
    - typecheck
  commit_msg:
    type: conventional-commits

ci:
  checks:
    - typecheck
    - lint
    - test

